Imports System

Module Program
    Sub Main(args As String())
        Dim n1% = Console.ReadLine()
        Dim n2% = Console.ReadLine()
        Dim n3% = Console.ReadLine()
        Dim p = (n1 + n2 + n3) / 3
        Console.WriteLine(p)
        Console.ReadLine()
    End Sub
End Module
